# Day 12 Practice Tasks
1. **Enum with Switch**: Create an enum `TrafficLight` with RED, YELLOW, GREEN, and an associated switch statement that tells a driver what to do.
2. **File Readers**: Write a class that uses `BufferedReader` to read the file created in the `FileIODemo`.
