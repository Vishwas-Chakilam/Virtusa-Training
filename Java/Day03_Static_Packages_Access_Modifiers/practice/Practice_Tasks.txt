# Day 3 Practice Tasks

1. **Math Utility**: Create a `MathUtils` class with `static` methods for basic math operations. Call them without instantiating the class.
2. **Access Control**: Create two classes in separate conceptual packages to test `protected` vs `public` access levels.
