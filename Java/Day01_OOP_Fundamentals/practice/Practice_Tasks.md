# Day 1 Practice Tasks

1. **Banking System**: Create a `BankAccount` class using encapsulation. It should have a private `balance` variable and public `deposit` and `withdraw` methods.
2. **Shapes**: Create a parent class `Shape` and child classes `Rectangle` and `Circle` that inherit from it.
