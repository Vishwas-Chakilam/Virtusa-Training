# Day 5 Practice Tasks

1. **Unique Elements**: Take an array of ints with duplicates, add them to a `HashSet` to retrieve only the unique elements.
2. **Generic Method**: Create a generic method that acts as a utility to print any generic array.
