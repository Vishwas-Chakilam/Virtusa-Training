Day 11: Exceptions
    
    Throwable -> Error (can't recover, like OutOfMemory) & Exception (we handle these)
    
    - Checked: compile-time forced handling (IOException).
    - Unchecked: RuntimeException (NullPointerException).
    
    try-catch-finally block. finally ALWAYS runs (good for closing resources).
    Or use try-with-resources (Java 7+) to auto close.
    
    // throws is in method signature, throw is to manually trigger an exception inside method.
    