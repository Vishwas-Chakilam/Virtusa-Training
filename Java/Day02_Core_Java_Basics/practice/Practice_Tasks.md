# Day 2 Practice Tasks

1. **Prime Number Checker**: Write a Java program to check if a number is prime using a `for` loop and an `if` statement.
2. **Calculator**: Write a program that takes two numbers and an operator (+, -, *, /) and uses a `switch` statement to perform the calculation.
