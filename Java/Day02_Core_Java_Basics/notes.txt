Day 2: Java Core

    Primitives taking up stack memory (fast!), Objects go to heap.
    - int, char, boolean, double... (8 total)
    
    Casting: 
    - widening is implicit (byte -> int)
    - narrowing is explicit (int -> byte) -> watch out for data loss here!
    
    Control flows: if/else, switch (can use strings now in newer java), for/while loops.
    
    // Remember: break jumps out entirely, continue just skips to next iteration.
    