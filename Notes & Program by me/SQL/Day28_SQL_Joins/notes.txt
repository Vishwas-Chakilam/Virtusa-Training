Day 28: SQL Joins
    
    Linking normalized tables back together.
    
    - INNER JOIN: only matching rows in both.
    - LEFT JOIN: all from left, matching from right.
    - RIGHT JOIN: vice versa.
    - FULL OUTER: all rows from both, nulls where no match.
    
    Cross joins = cartesian product (rarely used realistically?).
    