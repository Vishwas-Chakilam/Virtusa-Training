def very_long_function_name(arg_one, arg_two):
    # PEP 8 says max 79 chars per line, space after comma
    result = arg_one + arg_two
    return result

my_dict = {
    "first": 1,
    "second": 2
}